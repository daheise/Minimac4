language: cpp

complier:
  - gcc

install:
  - sudo apt-get install cmake python-pip python-dev
  - sudo pip install --upgrade pip
  - pip install --user cget

script:
  - cd Minimac4
  - bash install.sh
